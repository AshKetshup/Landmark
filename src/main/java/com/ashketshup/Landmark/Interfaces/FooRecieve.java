package com.ashketshup.Landmark.Interfaces;

/**
 * The interface Foo recieve.
 *
 * @param <T> the type parameter
 */
public interface FooRecieve<T> {
    /**
     * Apply.
     *
     * @param x the x
     */
    void apply(T x);
}
